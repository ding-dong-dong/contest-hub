# 推荐专业后端测试结果

- 测试日期：2026-09-11
- 测试人：陈玉婷（后端）
- 规则依据：田淋元《推荐专业规则与验收清单 V1.0》
- 数据来源：文博卉《推荐专业数据表_B版_20260911.xlsx》（30 条，imp_001~imp_030）
- Python：d:\contest-hub\.venv（3.12）；本地真实服务 uvicorn 冒烟通过；线上 Render 推送后自动部署

## 一、清单第 7.3 节必测项

- 数据库迁移：通过（启动自动 ALTER TABLE 新增 2 列；scripts/migration_recommended_majors.sql 已备）
- 15 条旧数据兼容：通过（迁移后旧 15 条接口返回 recommended_majors=[]、recommended_major_reason=null，无报错）
- 新增字段返回：通过（GET /contests、/contests/{id}、/contests/recommend 嵌套对象均含两字段）
- 多专业数组返回：通过（如 imp_001 返回 ["计算机","电子信息","理学","经管","文法","设计"]；30 条全部为数组，无字符串）
- 空推荐专业：通过（归一化 "待确认"/空 -> []；告警文案"推荐专业待确认"，不报错、不排除）
- 专业匹配加分：通过（用户专业命中 recommended_majors 专业项 20/20；相关/不限 12；待确认 8；兴趣另计 0-10）
- 不匹配但不限专业：未被错误排除（文法用户可见不限专业竞赛，imp_001 正常进入推荐）
- 明确专业限制：正确排除（计算机用户对 imp_016「医学及相关专业」资格判定 mismatch，不进入推荐；状态已截止的同时被硬过滤#1 拦截）
- OpenAPI 更新：通过（recommended_majors 为 array+items enum 10 值；recommended_major_reason string 可空、maxLength 150）

## 二、第 9 节联调画像实测（本地数据，2026-09-11 在报口径）

| 编号 | 画像 | 竞赛 | 实际结果 | 预期 | 结论 |
|------|------|------|----------|------|------|
| M01 | 计算机 | imp_015（规则样例） | imp_015 本届已截止，按硬过滤#1 不进推荐；同画像在报竞赛 imp_018（蓝桥杯）总分 73 排名第 1 | 专业命中并加分 | 通过（规则样例已过期，用在报竞赛验证同逻辑） |
| M02 | 文法 | imp_001（不限专业） | 推荐 16 条，imp_001 在列；列表筛选 28 条可见 | 不因未命中而排除 | 通过 |
| M03 | 经管 | 不限专业竞赛 | imp_001 等正常展示，无"资料不符"拦截 | 正常展示 | 通过 |
| M04 | 任意专业 | 推荐专业为空 | 代码路径单测通过（[] 时不报错并给"推荐专业待确认"告警）；B 版 30 条当前均有推荐专业，线上暂无空数据样本 | 页面不报错 | 通过（待前端联调展示文案） |
| M05 | 医学 | imp_016（限医学及相关） | score_contest 专业项 25/30（完全匹配 20+兴趣中性 5）；因本届已截止不进推荐，列表筛选可见 | 专业命中并加分 | 通过（下届开放后自动进入推荐） |
| M06 | 设计 | 不限专业/推荐为空 | 设计用户推荐 15 条；不依赖推荐专业做排除 | 正常展示 | 通过 |

附加：反向用例（计算机 × imp_016）major_eligibility=mismatch，硬过滤正确剔除。

## 三、真实 HTTP 冒烟（本地 uvicorn 127.0.0.1:8123）

- GET /contests -> 200，30 条
- GET /contests?major=医学 -> 200，28 条（含 imp_016/017，已截止竞赛在列表仍可见）
- GET /contests/imp_016 -> 200，recommended_majors=["医学"]，major_limit="医学及相关专业"
- GET /contests/recommend（计算机/AI/编程） -> 200，15 条，Top3：imp_018 73、imp_012 70、imp_010 70
- GET /contests/recommend（文法） -> 200，16 条，含 imp_001

## 四、已知问题 / 需跟进

1. 【流程】B 版 30 条 review_status 全部为"待审核"，按 V1.0 规则需田淋元复核为"可录入"后才算正式生效；
   本次按陈玉婷要求先行录入，脚本 scripts/import_b_version.py 可重复执行，复核后重跑即可覆盖更新。
2. 【数据】imp_029 推荐理由原文 439 字（规则上限 150），导入时已按句截断为 150 字并加"…"，
   建议退回文博卉精简；脚本每次运行都会对超长项告警。
3. 【规则】专业匹配分"完全匹配/相关/待确认"具体分值清单第 4 节标注"待定"，后端当前实现为
   20/12/8（另兴趣 0-10），田淋元确认分值后只需改 app/recommend.py 的 _major_interest_score。
4. 【数据】新 15 条的 organizer/materials/process/eligible_grades 等 B 版未提供，暂为空/[]，
   详情页需有缺省展示；推荐接口已按"资料未知按中性"处理，不影响出分。
5. 【线上】Render 免费实例首次请求可能休眠，属正常现象。
